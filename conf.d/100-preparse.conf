filter {
  if [reprocess] == "true" {
    prune {
      whitelist_names => [ "@timestamp", "original", "received_from", "type" ]
    }
  } else {
    mutate {
      rename => { 
        "message" => "original" 
        "host"    => "received_from"
      }
    }
  }
}
