filter {
  if [type] == "syslog" {
    grok {
      patterns_dir => [ "/etc/logstash/patterns.d/" ]
      tag_on_failure => [ ]
      match => { "original" => [ 
        "^%{EXTENDED_SYSLOG}$",
        "%{GREEDYDATA:message}" 
      ] }
    }
  }
}
