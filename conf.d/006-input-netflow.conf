input {
  udp {
    type => netflow
    #host => localhost
    port => 9995
    codec => netflow {
      versions => [9,10]
      target => netflow
    }
  }
  udp {
    type => ipfix
    #host => localhost
    port => 4739
    codec => netflow {
      versions => [9,10]
      target => ipfix
    }
  }
  tcp {
    type => ipfix
    #host => localhost
    port => 4739
    codec => netflow {
      versions => [9,10]
      target => ipfix
    }
  }
}
