input {
  file {
    path => "/var/log/logstash/reprocess/unknowns-*.json"
    codec => "json"
    sincedb_path => "/var/log/logstash/.reprocess-sincedb"
    # sincedb_path => "/dev/null"
    start_position => "beginning"
    add_field => { "reprocess" => "true" }
  }
}
