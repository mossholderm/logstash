SNORTALERT %{SNORT_TIMESTAMP:[snort][timestamp]}\s+\[\*\*\]\s+\[%{POSINT:[snort][gid]:int}\:%{POSINT:[snort][sid]:int}\:%{POSINT:[snort][signature_rev]:int}\] %{GREEDYDATA:[snort][message]} \[\*\*] \[Classification\: %{DATA:[snort][classification]}\] \[Priority\:\s+%{POSINT:[snort][priority]:int}\] \{%{DATA:[snort][proto]}\} %{IP_SRC}\:%{PORT_SRC} \-\> %{IP_DST}:%{PORT_DST}
SNORT_TIMESTAMP %{MONTHNUM}/%{MONTHDAY}-%{TIME}
