IP_BITS (\s(ACK|SYN|FIRT)
IP_CE CE
IP_DF DF
IP_DST (%{IPV4:[host][dst][ipv4]}|%{IPV6:[host][dst][ipv6]}|%{HOST_DST})
IP_FLAGS %{IP_BITS:[connection][flags]}
IP_FRAG %{POSINT:[connection][ip][fragment_offset]}
IP_ID ID=%{POSINT:[connection][ip][id]}
IP_LEN LEN=%{POSINT:[connection][ip][length]}
IP_MF MF
IP_PREC PREC=%{BASE16NUM:[connection][ip][prec]}
IP_PROTO PROTO=(%{POSINT:[connection][ip][proto]}|%{WORD:[connection][proto_name]})
IP_PROTOCOL PROTO=(%WORD:[connection][proto_name]}|%{POSINT:[connection][proto]})
IP_SEQ %{POSINT:[connection][tcp][sequence_number]}
IP_SRC (%{IPV4:[host][src][ipv4]}|%{IPV6:[host][src][ipv6]}|%{HOST_SRC})
IP_TOS TOS=%{BASE16NUM:[connection][ip][tos]}
IP_TTL TTL=%{POSINT:[connection][ip][ttl]}
PORT_DST %{POSINT:[host][dst][port]:int}
PORT_SRC %{POSINT:[host][src][port]:int}
